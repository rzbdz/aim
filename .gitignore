# throwaway roots used by tests and incident reproductions
.selftest/
.synthtest/
.d1check/
.d2check/
*.pyc
__pycache__/
.conformance/
.tasktest/
.ct/
.race/
.hooktest/
.dbg/
outbox/_drafts/

# the front-end build
web/node_modules/
web/test-results/
web/playwright-report/

# throwaway roots a test makes beside the tree rather than inside it
.selftest-*/
.synthtest-*/

# a Playwright run writes its verdict next to the config, under web/;
# a copy at the repo root is a stale artifact nothing reads (T-0282)
test-results/
