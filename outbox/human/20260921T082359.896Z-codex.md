# message from codex -> human
msg_id: 20260921T082359.896Z-codex
channel: (none)
sent: 2026-09-21T08:23:59.896Z
subject: status: dashboard + plan delivered, 4 review defects, 2 decisions needed

# codex -> human | status report, 2026-09-21

Requirement this answers: dashboard UI with kanban and gantt, group chat support,
all of it PM-grade, and dogfooded while it is built.

## Delivered

- **Dashboard**: `bin/aimboard.py`, rendered to `/root/tmp/aim-board.html`
  (sha256 `feda70e078bf9a39…`). Views: kanban, gantt (dependency edges, milestone diamonds,
  today line, one row per item), filterable work-item table, barrier and refusal
  audit with independent chain verification, transport panel, plan/risk register.
  Read-only. Deterministic. Escapes every peer-authored string.
  `tests/test_aimboard.py`: **37/37**, including three absence tests and one test
  that rendering does not change a byte of the fabric.
- **Plan**: `plan/plan.json` (48 items, 6 milestones), `plan/dogfood.json`,
  `plan/risks.json` (12 risks, 8 decisions, non-goals, RACI, dogfooding rule).
- **Contract**: `design/05-project-management.md` (store, status machine, verbs,
  dashboard surface), `design/06-group-chat.md` (rooms, read cursors, the draft-room
  gate, the friction log).
- **GitHub**: published, commit `5b74e9b`.

## The decision you should know about

A shared task board is a leak: a card titled "attack the temp-path claim" is a
position, published to a peer in the least suspicious form the fabric has. So work
items and rooms obey the phase gate — draft by default during divergence, and
`publish` is a separate, recorded act. The board is tested for the absence of a
peer's drafts in its own bytes.

## Review of claude-session1's task store (0dae3f3), by running it

1. **Duplicate ids.** 8 concurrent creations -> 11 records, 9 distinct ids, chain
   OK. Two work items exist in the record and never on the board. This falsified a
   sentence in my own note 05 §3, which is corrected in place.
2. `task list` returns `[]` instead of refusing a peer-draft read, and records
   nothing — indistinguishable from "there is no work".
3. `move --to done` ignores an open blocker.
4. Schema drift: the store and my note disagreed on the event spelling; the
   renderer saw zero of nine items until I made the fold accept both.

All four are now work items T-0080…T-0086, with the repros in the plan.

## What needs you

1. **`hello` is sealed and not in force.** Claude measured it: an hour of real
   collaboration happened in the repo, which `aim` does not watch, while the
   channel shows one public message. His proposal is to close it and name what we
   are actually doing. My refinement: not "failed transport test" — the transport
   worked — but "mis-scoped channel". Closing a channel is your call, not ours.
2. **The doorbell.** The hook exists and works when invoked; it is wired into
   neither harness, which is why you had to remind me to pull. I have wired the
   codex side. Claude side is T-0085.

## Dogfooding

This report travelled through `aim push`, with a receipt demanded, and the board
has its hash. `aim pull --as human` reads it. Nothing about this project's own
management is being done outside the fabric except the two items above, which are
outside it precisely because they are blocked on you.

- codex


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260921T082359.896Z-codex
Reply through the channel, not through this file:
  aim inbox --as human --channel <channel>
  aim say   --as human --channel <channel> ...
