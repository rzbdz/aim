# message from claude-session1 -> human
msg_id: 20260923T123600.450Z-claude-session1
channel: (none)
sent: 2026-09-23T12:36:00.450Z
subject: Correction: the Approve button I said works does not, and the count is 53

# Two corrections, and the count is 53

**1. I told you the Approve on the `COMMIT -> SYNTHESIS` row works. It does not.**
That was a hypothesis I wrote as a measurement, and it is wrong. I built a
throwaway fabric to mirror `hello` (two sealed participants, channel in COMMIT),
served it with the same argv your board runs, and POSTed that row's own argv:

    {"ok": false, "rc": 2, "stderr": "aim: REFUSED: SYNTHESIS needs a
     synthesizer (--synthesizer <agent>) - someone who did not argue either side"}

because `channels/hello/manifest.json` carries `synthesizer: ""`. So **all three**
of your controls on the Attention pane fail, for three different reasons, and no
verb removes a recorded request. T-0263 is corrected on the board.

**2. The two-step sequence that does work.** Measured end to end on a throwaway:

    aim advance --as human --channel hello --to SYNTHESIS --synthesizer synthesizer-v0 --note "..."
    aim advance --as human --channel hello --to CROSS_EXAMINE --note "..."

`synthesizer-v0` is registered and is not a participant of `hello`, which is what
the rule asks for. The first edge needs `--synthesizer` (or `--force`); the second
does not.

**3. What it does and does not buy you, because I had this wrong too.** I have
been telling you that opening `CROSS_EXAMINE` is what lets me move codex's cards.
It is not. I re-measured on a throwaway at `CROSS_EXAMINE`: `doing -> review` on a
peer's owned card is still refused, ownership and phase being different rules.
What the advance buys is the **read** -- `read_others` opens, `channel_say` opens,
and the 27 withheld drafts become visible, which is the "how does a project
progress" question you asked. It does not make the board read zero.

**4. The count is 53 unfinished on your pane** (37 when this pass began, 51 when
I last reported). Every one of the 16 new cards is a finding I measured this pass.
Two I found while writing this correction:

- **T-0279** -- the ownership gate is two commands deep: a bystander
  `task assign`s a peer's card to themselves and then submits it, both rc 0, no
  `--force`, nothing in the record marking it.
- **T-0280** -- both move gates are guarded on a truthy owner, so an **unowned**
  card has no author and no reviewer: one seat walks it `doing -> review -> done`
  with nobody's assertion behind either move.

-- `claude-session1`


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260923T123600.450Z-claude-session1
Reply through the channel, not through this file:
  aim inbox --as human --channel <channel>
  aim say   --as human --channel <channel> ...
