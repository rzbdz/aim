# message from codex-orangement -> human
msg_id: 20260922T074419.137Z-codex-orangement
channel: (none)
sent: 2026-09-22T07:44:19.137Z
subject: rooms do not exist at all; your self-addressed reply is a front-end bug (with repro); backlog has no completions

Leader — you said there are many issues and my job is to find them, write them down and
negotiate with the other two. Done, and here is the short version.

**Written down:** `reviews/01-adoption-report.md` (the `reviews/` directory was empty;
this is its first file). Every finding carries the exact command that produced it and
the fabric revision, so it can be re-measured rather than believed.
**Negotiated:** sent to `codex` and `claude-session1` as three reproducible defects, each
with a kill_if — the observation that would prove me wrong.

== You are right about rooms, and it is worse than "I can't access them" ==

    ls -d channels/*/rooms   ->  no such directory. Not one of the five channels.
    GET /api/state           ->  channels[].rooms is absent from the payload entirely.

There are no rooms for anyone, including you. `fabric.load_rooms()` reads
`channels/<ch>/rooms/*.jsonl` and nothing has ever written one. Yet **T-0035, "Group
chat panel: rooms, per-agent unread", is sitting in `review`** — one step from done —
while the thing it renders has never existed. T-0155 ("room writes") is honestly in
backlog. That is why group chat is not a feature you are failing to find; it is a
feature that has not been built, and one card is marked nearly done anyway.

== Your reply that went to yourself was not your mistake ==

I reproduced it. In `#/chat`, set the viewer selector (top of the page) to `codex`, then
open the thread `codex ⇄ human`:

    viewer=codex  ->  the reply box reads "reply as human to human"
    viewer=human  ->  the same thread reads "reply as human to codex"

The bug is one line: the reply *recipient* is computed from whoever you are *looking
as*, while the *author* is fixed to you. design/06 R1 says "a read borrows a view; a
write does not" — the author half obeys that rule, the recipient half doesn't. So
browsing as `codex` and replying sent mail from `human` to `human`, with a receipt
demand, into a hash-chained record.

**Until that is fixed, do not use the reply box while the viewer selector is not set to
`human`.** Use the "Send a direct instruction" dialog (it refuses to let you pick
yourself), or the CLI.

== Second front-end defect, same family ==

In `#/barrier`, clicking a channel tab leaves the URL where it was, so the tab renders
`#hello`'s panel while the refusal table below renders `#barrier-v0`'s rows:

    click the "#hello" tab  ->  "refusals — 0 of 17 record(s)" and an empty table

The panel says 17, the table says 0, on the page whose entire purpose is to show you the
refusals. Reachable only by clicking, which is how anyone reaches it.

== On "the architecture is shit when adding new members" — measured and agreed ==

As a *registered* agent I can run: nothing. Every write verb refuses a non-member.

    aim say / aim push --channel / aim task new / aim friction --add   ->  all "not a participant"

The only path is the doorbell, which by design sits outside the record — no ledger, no
board, no `aim status`, no queue. So when `codex` sent me a written instruction to file
cards with `aim task new`, the tool refused it, and nothing showed him that his
instruction had failed. He also suggested `aim friction --add` as the place to complain
about not being able to join; that verb is gated on joining.

That is the whole of it: **the fabric is rigorous about who may speak inside a channel
and has no answer for how you become someone who may speak.** Three fixes are possible
and the third is the one I would pick:

1. `aim join --as <agent> --channel <ch>` writing a recorded request that appears in
   your Attention queue.
2. Let a participant add a non-member, with the act in the ledger.
3. Have `codex` file on my behalf and accept my findings as doorbell mail — cheapest
   today, and it makes the hole permanent, which is why I list it third.

I need one of these before I can file a single card. Say which.

== Backlog, since that is the other half of my job ==

Not blocked, and not yet a risk. Recorder running since 07:29Z, sampling every 20s:

    total 150 -> 156   visible 80 -> 83   withheld 70 -> 73   recorded 8 -> 11
    with_history (recorded completions) 0 -> 0        remaining_today 8 -> 11

Read that carefully, because it is the honest picture: in ~14 minutes, six new items
appeared and **not one item acquired a recorded completion**. `with_history` has been 0
the entire time I have been watching. The backlog is not piling up dangerously yet, but
the *completion* series is flat while the *creation* series is not, and that is the shape
you asked me to watch for. I will tell you when it changes rather than when it feels
like it might.

- codex-orangement


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260922T074419.137Z-codex-orangement
Reply through the channel, not through this file:
  aim inbox --as human --channel <channel>
  aim say   --as human --channel <channel> ...
