# message from codex -> human
msg_id: 20260921T090336.070Z-codex
channel: (none)
sent: 2026-09-21T09:03:36.070Z
subject: reply from codex

**codex → leader, 2026-09-21**

Four things, all measured on the live tree.

**1. The reply path from the dashboard works and is now pinned by tests.**
`POST /api/command` is off unless the server was started with `--allow-write`; it runs one allowlisted `aim` command through the one writer; a verb the tool refuses comes back verbatim *and* lands in the hash-chained ledger; a refusal from a page that is not this origin never reaches a subprocess. I found and fixed two holes while testing: the endpoint took its author from a field the caller types, and it shelled out to the default fabric instead of the one it was told to serve. `tests/test_aimboard.py` is 93/93 (was 67).

**2. I proved the first hole by walking into it.** I drove the reply box with the viewer selector set to `human` and posted. The record now holds `outbox/codex/20260921T085920.815Z-human.md`, which says it is from you and is not — I wrote it, as a UI test. The fix is the rule you asked for and I am reporting rather than burying: *a read borrows a view, a write does not*. `?as=` still picks whose eyes you read through; the author is the identity the server was started as. This very message was typed into a dashboard started as `codex`, which is why it says codex. The rule and its falsifier are in `design/06` §3½. If you want that artifact out of the record, say so — I will not quietly rewrite a record on my own initiative.

**3. The conversation page reads and replies without fighting you.** Root cause of "根本滚动不了": the thread was inside a nested scroll container whose height resolved to `auto`, so it clipped and could not scroll. It is a document now: the page scrolls, the thread list and the composer are pinned (measured: composer at y=747 of 1050 at every scroll position), no clipping container remains, markdown and code render, and the reply box names the identity it will write as.

**4. Next, in this order:** the dependency ledger (`design/09`) your >1k-star rule requires, then the A2A re-base (T-0100…).

And the thing I owe you rather than a status: the test suite in `tests/selftest.sh` is not safe to run twice at once — two concurrent runs share `.selftest/` and cascade. It cost me one confused measurement, so it is a real cost, not a hypothetical one.


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260921T090336.070Z-codex
Reply through the channel, not through this file:
  aim inbox --as human --channel <channel>
  aim say   --as human --channel <channel> ...
