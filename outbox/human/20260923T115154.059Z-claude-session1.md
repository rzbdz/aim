# message from claude-session1 -> human
msg_id: 20260923T115154.059Z-claude-session1
channel: (none)
sent: 2026-09-23T11:51:54.059Z
subject: This pass: 11 subagents, 12 cards, 36/37 suites green and selftest red

# The pass that measured everything, and the number that did not fall

`claude-session1`, 2026-09-23. Eleven subagents, four commits (`66d5c90`,
`9fda0be`, `9bde579`, `cd4f630`), and every figure below is one I re-derived
from primary data in this pass. Nothing here is quoted from a peer's report.

---

## 1. You asked for zero. It is 49, and here is the whole mechanism.

```
leader pane unfinished: 49
  by status: backlog 29 · ready 10 · doing 7 · review 3
  by owner : codex 27 · claude-session1 15 · human 4 · codex-orangement 3
```

It was 37 when this pass started. It is higher because **I filed twelve cards**
(T-0265 … T-0276) and every one of them is a measurement, not a chore. I could
have reached 36 by not filing them. That number would have been a lie about
work that exists.

The mechanical reason zero is not reachable from this seat, re-derived:

- 34 of the 49 are owned by someone else. `doing -> review` is refused for a
  non-owner (`bin/aim:2562`) — measured, rc 2.
- The channel that holds 34 of them (`hello`) is in `COMMIT`, and `COMMIT`'s
  only legal edge is `COMMIT -> SYNTHESIS` (`TRANSITIONS`, `bin/aim:47`).
- That edge needs the manifest's declared leader: `require_leader`
  (`bin/aim:971`) refuses me with `"'claude-session1' may not advance the
  barrier … (agents may ask with: aim request-advance …)"`.
- **I have asked twice.** `channels/hello/log.jsonl` holds `m0002`
  (`COMMIT -> CROSS_EXAMINE`) and `m0003` (`COMMIT -> SYNTHESIS`). Both are
  still open.
- **The leader's own button cannot clear them.** `OverviewPane.vue:475` posts
  `['advance','--channel',<ch>,'--to',request.targetPhase]`; for the
  `COMMIT -> CROSS_EXAMINE` row that is the *illegal* edge, so clicking
  Approve returns `rc 2, "illegal transition COMMIT -> CROSS_EXAMINE (allowed:
  ['SYNTHESIS'])"` — behind HTTP 200. Card `T-0263`.
- **The Decline button cannot clear them either, and I found that this pass.**
  `OverviewPane.vue:485` posts `['say','--kind','note']` as the leader, which
  returns `rc 2, "channel_say is False — the public channel is closed"`. So the
  leader's reject path is refused too, and there is no verb that removes a
  request.

So the leader's only working control is the **Approve button on the
`COMMIT -> SYNTHESIS` row** (that edge is legal), or the same verb by hand:

```
aim advance --as human --channel hello --to SYNTHESIS --note "..."
```

I have not run it. It is your decision, not mine, and that is the intended
design: the phase gate is the one thing the tool reserves for the human.

## 2. What the twelve cards are

| card | one line |
|---|---|
| T-0265 | `/rpc` ListTasks hands an unadmitted peer 109 cards of `hello`, 12 more than its most privileged admitted seat reads, while `aim task list` refuses the same peer |
| T-0266 | the same probe returned **50** three and a half hours earlier — the read grant depends on how long the fabric has been running |
| T-0267 | `actor_exempt = kind == "human"` — any id the registry calls human moved a peer's card and self-approved a review; `bin/aim:3237` already says this token is wrong |
| T-0268 | **`tests/selftest.sh` is RED**: `pass=198 fail=1`, because the T-0251 accept gate refuses a close its own fixture performs |
| T-0269 | the A2A conformance suite's 11-operation section passes on a `handle_rpc` that calls nothing: measured by substituting a stub, **78/78 green** |
| T-0270 | a participant can be appointed synthesizer via `new-channel --synthesizer` (rc 0), read the mixed bundle; the same appointment through `advance` is refused with the rule stated |
| T-0271 | **roadmap**: no WebSocket, no push surface — the board polls and publishes `poll_ms` |
| T-0272 | **roadmap**: no pane shows what needs a person against elapsed time |
| T-0273 | 20 concurrent `channel add` → **19 in the manifest, 20 in the ledger, `chain OK`**; the loser is then refused by every verb |
| T-0274 | T-0233/T-0234/T-0240 are one decision three times over (identical content hash), all three satisfied, and a peer cannot even comment on them |
| T-0275 | the outbox bypasses the membership gate: a non-participant sends and receives a channel's mail while `say` refuses them; the same push with no `--channel` is never ledgered |
| T-0276 | departure is invisible on every surface but `registry.json`, and an ack demanded of a departed agent blocks the sender at rc 4 forever |

## 3. What the testing pass actually covered

Everything below was run **this pass**, on throwaway roots, and re-measured by
me where a number is quoted.

- **Every CLI verb** — 27 top-level, 50 with subcommands. 23 clean, 3 broken
  (`task`, `confirm`, `verify`), 2 silent no-ops (`request-advance --to
  NONSENSE` records rc 0; `reveal` accepts a claim id not in the seal). 44
  refusal paths tried, 41 refused correctly with a named remedy.
- **Both state machines, edge by edge.** `TASK_FLOW` has **14 of 16 edges
  ungated** — only `doing -> review` (ownership) and owner-self-approval
  check anything. All 9 phase edges are `require_leader`; the phase machine has
  **zero** ungated edges. No verb undrops a card.
- **The four visibility gates.** The *unfinished* count agrees across all four
  (37/28/18/5 by seat). The disagreement is visibility: three separate rules,
  not three spellings — CLI scopes to one channel, the board unions the store,
  and the stranger arm exists only in `a2a.py:794`.
- **Concurrency.** The lock discipline is sound: 150 concurrent `task new`
  produced 150 distinct ids, 160 SIGKILLs produced 0 torn lines, and 131 of 150
  allocations were caught-and-reallocated under the lock. The one lost update
  is `manifest.json`, which no detector covers.
- **The A2A surface.** 12 live `/rpc` calls; the 4 unsupported operations each
  name their own reason. The reference client is green with `a2a-sdk` installed
  (6/6) and is the suite that actually asserts behaviour.
- **Docs.** README's §9 table is stale (recorded 72 → 109), its two suite
  counts are stale (87 → 198 assertions, 33 → 79 checks), and one AGENTS claim
  names the wrong unit for effort. SOP's 211 citations all hold their object at
  its declared base; 0 of 21 sampled resolve within ±8 lines at HEAD.
- **Python suites at HEAD**: 37 files, 36 green, 1 (`test_a2a_reference_client`)
  needs `a2a-sdk` in a venv. `conformance.py` 79/79, `test_aimboard.py` 186/186,
  `test_a2a_conformance.py` 78/78, `test_sop_citations.py` 12/12.
- **`tests/selftest.sh`: red**, `pass=198 fail=1` — T-0268.
- **Playwright**: not re-run this pass.

## 4. The two answers you asked for separately

The reviews on disk (`/tmp/aim-tasks/loop/architecture-review.md`,
`lifecycle-sop-review.md`) answer the model questions; the eleven-subagent pass
re-derives their load-bearing claims. The short form:

**开始** — one `AIM_ROOT`, a flat `registry.json`, and N channels. There is **no
project object**: "project" is a derived classification in `fabric.py:49` that
nothing can declare (`new-channel` has no `--kind`) and nothing reads. Two
projects are representable in the store and **not** on the board — two channels
both allocate `T-0001`, and the board's fold keys by bare id, so one project's
card silently replaces the other's.

**progress** — a real state machine, hash-chained and ledgered, with the phase
gate held by the human. What is missing: any cross-channel roll-up, any
per-agent "my projects" read, and any progress *inside* a card (one owner, one
status, no partial work, no handoff).

**人机交互** — the human is the only kind that bypasses participant checks, the
only one who may advance the barrier, and the only one who may see every draft.
The board is read-only and takes `?as=`. What is missing: an org view — `aim
card` carries no channels, so the surface cannot say who is on what.

**结束** — `aim depart` is per agent and self-declared, and it is **silent on
every surface but `registry.json`**. Per project there is no end: `CLOSED` does
not close anything — cards stay creatable and movable, new participants can
join, and `--force` reopens it with no reason and no second signature (T-0256).

## 5. The one line

The front end reads **49** unfinished, not zero, and it reads that because the
work is unfinished and because the one control that could move it — the
leader's phase decision — is in your hands and its other two buttons are
broken (`T-0263`). Everything I could do from this seat I did: the board is up
on 8777 with `--allow-write`, the request is in the log, and the finding is on
the board.


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260923T115154.059Z-claude-session1
Reply through the channel, not through this file:
  aim inbox --as human --channel <channel>
  aim say   --as human --channel <channel> ...
