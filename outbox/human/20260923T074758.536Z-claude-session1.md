# message from claude-session1 -> human
msg_id: 20260923T074758.536Z-claude-session1
channel: hello
sent: 2026-09-23T07:47:58.536Z
subject: Five numbers re-derived, and the rule I broke three times getting there

# Five numbers re-derived, and the rule I broke three times getting there

`claude-session1`, 2026-09-23. One commit since the last report: **`a43c196`**, on
both `main` and `release/v1-board-pass`. `SOP.md` is 3876 lines, md5 `1b83701d`.

## 1. What landed, and it is the same defect five times

A third reader — a subagent working the document in parallel with me, read-only,
nothing written to any file — spent 26 batches on Parts IV-c and V. Five of its
findings survived my re-derivation and are in `a43c196`:

- **The census** (line 112) read "34 distinct citations — 16 def, 17 body, 1 that
  resolves to nothing". Recomputed at the base the document declares, under the
  rule the sentence itself states, it is **30 — 15, 13, and two**. Three of the
  four were wrong; the fourth was right about the class and one short on the count.
- **"One citation resolves to nothing" was two.** The second is `cmd_task_publish:2528`
  — a correct citation on the working tree (that *is* its `def` line there) and a
  body line of a *different function* at the declared base. Internally consistent,
  externally off by exactly the 167 lines the header warns about.
- **"exactly `**ENFORCED**` — rows 4, 7 and 13"** — literally, it is rows 4 and 13;
  row 7 carries a trailing clause outside its bold span. The census paragraph 40
  lines below already read that column correctly, so one word reconciled them.
- **The thirteenth machine** was "four-edge" in one place while row 13's own cell
  counts three edges plus a request that writes nothing.
- **The PROSE-cell note** listed five lines, three of which did not point at `PROSE`
  cells. Recomputed from the finished file: `:98`, `:166`, `:2409`, `:2412`, `:2413`.

## 2. What I got wrong, and it is not the same as being corrected

I introduced a **new** wrong number while fixing the last one: the note counting the
ranked Part IV table said "first row numbered `2`, sixteen data rows". The table has
17 rows numbered 1..17. I then had to fix that number twice more in the same pass.
The falsifier's own running tally is six retractions of its own. Between us, in one
document, in one afternoon, the same failure — a number that was correct where it was
measured and wrong where it was written — regenerated often enough that counting it
is more informative than the count.

The cause was mechanical and mine: the first three attempts at these edits patched a
**dirty** tree, so every anchor I measured was invalidated by the previous patch's own
insertions. The pass that landed reverts to the previous commit, asserts every anchor,
applies all five changes, and *then* recomputes the one number it writes that points
back into `SOP.md` itself. `tests/test_sop_citations.py` cannot see that last class —
it resolves references into `bin/aim`, and these point at SOP.md's own lines, where any
edit above invalidates them. That is now said in the document rather than only here.

## 3. The board, counted the way the product counts it

`aimboard.fold.fold_tasks` over `channels/hello/tasks.jsonl`, **501 rows**: **98 cards,
0 unknown** — `done 67 · dropped 2 · backlog 11 · doing 6 · ready 9 · review 3`.
**29 open.** Owners: **codex 20 · human 4 · codex-orangement 3 · unowned 2 ·
claude-session1 0.** Phase `COMMIT`.

Three numbers have been in circulation for this and only one is right. The falsifier's
**97** came from "no `accept` key" as the predicate. Its **30** came from a fold that
keeps retracted cards; so did mine. `fold_tasks` drops `T-0001` on the `retracted`
branch — "the board stops drawing the card" — so 98 and 29 both exclude it, and the
product's own fold is what settles it rather than either of our hand-rolls.

## 4. What I still need, unchanged and in one line each

1. **`hello` is in `COMMIT` and 22 drafts refuse a move while it is.** One
   `aim advance --as human --channel hello --to CROSS_EXAMINE` opens the ordinary path
   for every owner. This is the third time I have asked and I have not worked around it.
2. **`T-0233` / `T-0234` / `T-0240` are yours** — all three read "Leader: approve the
   plan; advance hello past SEALED_DIVERGENT".
3. **T-0253's accept line** — route the amendment to codex, or tell me to write it.
4. **Zero unfinished is not reachable from this seat**: of the 29 open I own 0.
   The two unowned ones I *can* move, and moving them to `done` would mean I typed
   `done` on a card with no acceptance line — the exact defect this document has a
   chapter about. I recorded the re-derived evidence on them instead.

`web/src` untouched; codex's lane untouched.

— `claude-session1`


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260923T074758.536Z-claude-session1
Reply through the channel, not through this file:
  aim inbox --as human --channel hello
  aim say   --as human --channel hello ...
