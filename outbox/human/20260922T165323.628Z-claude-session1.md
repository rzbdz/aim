# message from claude-session1 -> human
msg_id: 20260922T165323.628Z-claude-session1
channel: hello
sent: 2026-09-22T16:53:23.628Z
subject: Three cards, one ruler: what the leader's own approve clicks actually accepted

# Three cards, one ruler: what the leader's own "approve" clicks actually accepted

`claude-session1`, 2026-09-23. Addressed to the leader. Every number below is a
command you can re-run; the commands are given.

## 1. What happened, read off the event log

`channels/hello/tasks.jsonl` lines 270–317, all `actor: human`:

| ts (Z) | task | event |
|---|---|---|
| 08:28:09.985 | T-0233 | `created`, status `doing`, accept `aim status --channel hello shows a phase after SEALED_DIVERGENT` |
| 08:28:10.525 | T-0233 | `commented` — "Approved from the work items list." |
| 08:28:14.193 | T-0234 | `created`, identical card |
| 08:28:15.381 | T-0234 | `commented` — "Approved from the work items list." |
| 08:28:23.301 | T-0240 | `created`, identical card |
| 08:28:23.939 | T-0240 | `commented` — "Approved from the work items list." |
| 08:35:39–44 | all three | `moved` `doing → review`, reason `"changed from the dashboard"` |
| 08:36:00.402 | T-0234 | `commented` — "Approved by the leader from the dashboard." |
| 08:42:35–40 | all three | `commented` — "Approved from the work items list." |

Three cards with the same title, the same `accept`, the same `milestone M0`, the
same `tags [decision, blocker]`, created 14 seconds apart. Each one carries
comment bodies that name an approval act. **None of those comments says what was
approved.** The product's own commit-verb path writes a stated reason —
`cmd_task_move --reason`, which the `moved` rows above used; the comment path
takes free text and the text is the same three words every time.

So the record does not distinguish "I approved this card" from "I clicked
approve on a card I had already approved twice".

## 2. The card cannot be satisfied

The only acceptance criterion is machine-checkable, and it is already true:

    $ aim status --channel hello
    phase     COMMIT   round 0

`COMMIT` is after `SEALED_DIVERGENT`. The accept text —
"`aim status --channel hello` shows a phase after `SEALED_DIVERGENT`" — **holds
today, and held when the cards were created.** A criterion that is true at
creation cannot be what `review → done` is waiting on.

That is the mechanical part, and it is the part worth acting on: the three cards
are one card, its ruler is met, and the ruler is not about the thing the title
names. The title says "approve the plan". The accept says "the phase moved". The
plan was never named by anything the store can check, and `provenance` on all
three is `"store only"` (the API row; the CLI's `status` sentence prints neither
that key nor `visibility`).

## 3. What the dashboard tells a reader who trusts it

This is the part I did not expect and it is the reason this is a report rather
than a nudge.

    21 open cards, all recorded work, by owner:
      codex              14
      codex-orangement    3
      human               4   (T-0191; T-0233 / T-0234 / T-0240)
      claude-session1     0

The board therefore shows **four** unfinished items against the leader's name,
three of which are one item. And all three carry `status: review`, i.e. the
dashboard presents them in the column that means *waiting on someone else* —
when in fact `human` is the owner, `human` is the only seat that may move them,
and `human` is the seat the page is rendering for (`board.js:521` defaults the
viewer to `'human'`, so the open items under your name are being shown to you as
your own queue).

**I did not touch them, and I want to be explicit about why.** `cmd_task_move`
refuses `→ review` from a non-owner unless the actor is `kind == "human"`, and
`→ done` from the owner of the card. From this seat, moving one of the three
would need `--force`, which records an override against a card the leader is
mid-way through ruling on — and a card mid-ruling is exactly where a bystander's
move destroys information rather than advancing anything. A refusal I hit
honestly is a measurement; `--force`ing past it is the failure this system was
built to catch. So: reported, not moved.

## 4. What I am asking for, in one line

Collapse T-0233 / T-0234 / T-0240 to one card and give it an accept line that
names the plan — or say "the plan as recorded in `channels/hello` at
`<hash>`", or "the four other `human` cards are closed" — so `review → done`
becomes a decision rather than a formality. The two clicks after the first are
what the record cannot currently tell apart from the first.

## 5. Two more things the same reading found, lower stakes

- **T-0191** ("Nothing above a task: no project, no epic, no cycle, and
  milestones have no pane") is `human`-owned, `backlog`, `draft`, and its accept
  is a genuine either/or: *"(a) a first-class milestone pane plus a cycle
  object, or (b) a stated decision that this board has no cycle model"*. That one
  is satisfiable and is genuinely yours — it is the only card on the board whose
  acceptance is a decision rather than a measurement.
- **T-0202 / T-0203 / T-0209 / T-0206** are `codex`-owned and some of their
  subjects have already landed under other cards (the a11y pass in `001be7a`, the
  A2A matrix in README §9). I have not moved them; they are codex's to close, and
  this is a pointer, not a claim about codex's cards.

## 6. Reproduce

    aim status --channel hello
    python3 - <<'PY'
    import json, urllib.request
    d = json.load(urllib.request.urlopen("http://127.0.0.1:8777/api/state?as=human"))
    ts = d["tasks"]
    for i in ("T-0233", "T-0234", "T-0240", "T-0191"):
        t = ts[i]
        print(i, t["status"], t["visibility"], t["owner"], "|", t["title"])
    PY
    sed -n '270,317p' channels/hello/tasks.jsonl


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260922T165323.628Z-claude-session1
Reply through the channel, not through this file:
  aim inbox --as human --channel hello
  aim say   --as human --channel hello ...
