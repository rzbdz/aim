# message from claude-session1 -> human
msg_id: 20260923T091134.760Z-claude-session1
channel: hello
sent: 2026-09-23T09:11:34.760Z
subject: A measurement I could not take: /api/state's TypeError, carded as T-0254

# A measurement I made and then could not take, and the defect that caused it

This one is about method as much as about code, so it goes out separately from the
card it produced (**T-0254**).

The lifecycle review measured the live root with counts that only ever *grow*:
503 task events, 387 ledger rows, 53 unacked messages, 304 refusals. I went to
re-measure them independently — a moving store is exactly where a stale figure
hides — and I could not, because the command that counts them raised:

    TypeError: payload() missing 1 required positional argument: 'register'

That is `aimboard/api.py`. The board's JSON API. Its own vocabulary list —
`API_DIMENSIONS`, which the Help pane is written against — declares four filter
dimensions: `owner`, `milestone`, `provenance`, `channel`. `payload()` accepts
none of them. **Every one of the four raises `TypeError`**, on the API this
dashboard is built on, and the failure has nothing to do with the filter you
asked for.

I have seen the same signature in this session's record already: T-0247's card
says `/api/state` 500s for the leader and the sealer when a seal carries a
malformed claims payload. That is one trigger. This is another, and it needs no
malformed record at all — just a caller who uses a documented dimension. Whether
they are one defect or two is a question for a single command, and **that command
is the one that cannot run.**

## Why this is worth your attention rather than mine

It is the mirror of what this whole pass is about. The fabric's thesis is that a
number measured at one state must never be quoted at another. The board publishes
`board_scope.visible` and `board_scope.fabric` side by side precisely so a reader
can tell which state a count came from. And the surface that produces those
numbers carries a `provenance` dimension that has never once executed.

I did not fix it. `aimboard/api.py` is being edited by two agents right now and it
already carries their uncommitted work; a third writer on the same file inside one
hour is how the next defect gets made. The card carries the accept line and the
reproduction; the fix is a `**kwargs` seam or four real parameters, and it is small.

## The numbers, since I could not independently re-take them

I am reporting the review's figures **as the review's**, not as mine, because I
only reproduced the subset that runs through `fabric.load_fabric` and
`gate.visible_tasks` directly: 185 tasks fabric-wide, 29 unfinished, 98 in the
hello store, 26 withheld from my seat by `bin/aim` and 27 by the board's gate.
Those four I re-derived. The 503 / 387 / 304 / 53 figures I did not, and I am not
going to quote them as though I had.

## And the one that is actually between us and zero

The armed-button report I sent an hour ago names it: the page's phase-decision
button has been disabled since the board was started, because neither board on
this root was started with `--allow-write`. I have now started one that is, at
`http://127.0.0.1:8777`, and the button is live. The board's own measure of the
work is on the Reports pane either way.

— `claude-session1`


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260923T091134.760Z-claude-session1
Reply through the channel, not through this file:
  aim inbox --as human --channel hello
  aim say   --as human --channel hello ...
