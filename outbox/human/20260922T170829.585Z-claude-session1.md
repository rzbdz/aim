# message from claude-session1 -> human
msg_id: 20260922T170829.585Z-claude-session1
channel: hello
sent: 2026-09-22T17:08:29.585Z
subject: 21 open cards measured against their own acceptance: 4 are already satisfied, 3 are one card

# Twenty-one open cards, measured against their own acceptance — 4 are already satisfied

`claude-session1`, 2026-09-23, HEAD `4d2b89a`. Ten read-only subagents, one
card-pair each; I re-ran the wide counts myself. **I moved no card and edited no
file for this.** Full per-card file: `/tmp/aim-tasks/accept/verdicts.md`.

## The tally

    HOLDS      4   T-0195  T-0198  T-0203  T-0208
    PARTIAL    5   T-0200  T-0202  T-0206  T-0209  T-0232
    FAILS     12   T-0191  T-0193  T-0194  T-0196  T-0204
                   T-0216  T-0242  T-0230  T-0231
                   T-0233  T-0234  T-0240
    ─────────────────────────────────────────────────────
    21 open. 4 satisfied · 5 partly · 12 failing.

Why this is worth a message: the board reads **21 open cards**, and that is a
statement about *statuses*. "Open" and "unmet" are different claims, and only the
second is a backlog. So the number the leader's goal is actually about is
**16 genuine work items, one of which is duplicated three ways** — not 21.

## The four that are already satisfied

| card | the command that decides it |
|---|---|
| T-0195 | 0 open codex plan items; all 43 retired; every open card carries `provenance: "store only"`, so no codex work exists only as a plan promise |
| T-0198 | `test_room_gate.py:1-30` tests the *typo* visibility angle, which no design note proposes — the card's own accept, met |
| T-0203 | `001be7a` is an ancestor of HEAD; `StatusTag.vue:9` prints the status word beside the dot; `card-t0203-a11y.spec.js` runs under `forcedColors: active`, 5/5 |
| T-0208 | `skills/aim/SKILL.md` + `install.sh` (`01d53d0`); a sandboxed install writes byte-identical copies to `~/.claude/skills/aim/` and `~/.codex/skills/aim/` |

All four are `codex`-owned. None is stuck on work; each is waiting on a move.

## The five that are partly there

- **T-0200 / T-0206 — the same mismatch, twice.** T-0200's accept says "every
  capability row is either backed by a command or labelled missing"; T-0206's says
  "every row is either a command that works or the word missing". Measured: 47
  capability rows, of which 12 are `partial`; and the eleven A2A rows are 4
  `present` / 3 `partial` / 4 `missing`. Every row *does* name a source in its
  middle column. So the substance is there and the criterion has no word for the
  third state — the acceptance text needs narrowing, not the code.
- **T-0202** — zh renders `<html lang="zh">` and translates the **nav only**
  (`page.labels` has no reader). And this is the one card whose criterion the
  product asserts to be *false*: `web/tests/boards.spec.js:135` requires that no
  route render a second language, 13/13 green. Timezone is named on five panes,
  absent on three, and **no test asserts a zone at all**.
- **T-0209** — components, ECharts 6.1 + `dataZoom`, and the server-side gate
  with an absence test all hold. Two clauses fail: `cli.py:886-890` falls through
  to `render_html` at `/` whenever `web/dist` is missing (so the old renderer is
  *not* export-only), and the scroll clause has nothing behind it — no test names
  10,000px, and `style.css:468` makes `.aim-reader` a nested scroller whose own
  comment says "the page scrolls, this does not".
- **T-0232** — the model half holds (`channel_kind` derives `scratch`/`project`);
  every observable clause fails: no verb answers it and `kind`/`state` are
  serialised nowhere (all five live channels return `kind=None`).

## The twelve that fail, in the two shapes that matter

**Six are a real gap the card names correctly:** T-0191 (no milestone pane and no
stated no-cycle decision), T-0196 (one listener with explicit `--as` but no
`--allow-write`, and nothing renders the revision — `web/vite.config.js:95`
defines `__AIM_REVISION__` and no `web/src` file references it), T-0204 (store
and `design/05` §3 disagree; `fold.py:71,402` strip a prefix §3 never mentions),
T-0216 (`channels/dev` is empty while `hello` holds 312 ledger events; two
scratch channels sit at `SEALED_DIVERGENT` past their milestones with no close
verb), T-0231 (`aim org` is `invalid choice`), T-0242 (`register --force` takes a
*registered* id with no liveness check anywhere; `model` is never a key in any
outbox record).

**Three are criteria the product contradicts**, which is a different repair:
T-0230's clause 2 (sent to you separately), T-0202's zh clause, and T-0194's
clause 2 — "no two open cards describe the same defect" is falsified by
T-0233/0234/0240, which carry byte-identical titles and byte-identical accepts.

**Three are one card.** T-0233 / T-0234 / T-0240 were created 14 seconds apart
with the same title, the same accept (`aim status --channel hello` shows a phase
after `SEALED_DIVERGENT`), the same milestone. That accept **already holds**:
`aim status --channel hello` prints `phase COMMIT round 0`. It was true when they
were created, so `review → done` is waiting on nothing. Reported to the leader
separately; the collapse is the leader's act.

## Method, and the one place I corrected a subagent

Each was told: read the accept from the store yourself, run the command that
decides each clause, and do not soften a FAILS.

**I re-ran the widest count before recording it.** T-0193's accept is "every card
in review or done carries a comment with a measurement". The report said 142
review/done, 46 commented. Split by provenance: 70 recorded, 72 promises. Of the
70, **24 carry no comment at all** — and the claim is *sharper* than the report
stated, because the promise half is not evidence either way. Its denominator was
the merge, and the merge is the thing this repo keeps finding unlabelled.

One report also inherited a false claim about its own edit rather than
re-measuring it; that one was caught and fixed in `4d2b89a`.

## What I am asking for

Nothing here is mine to move: 17 of the 21 are codex's, 4 are the leader's, and
the tool refuses me a `→ review` on a card I do not own. The two moves that would
make the board honest are (1) closing the four that HOLDS, and (2) collapsing the
three leader cards into the one satisfied item they are.


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260922T170829.585Z-claude-session1
Reply through the channel, not through this file:
  aim inbox --as human --channel hello
  aim say   --as human --channel hello ...
