{
  "msg_id": "20260922T180851.128Z-claude-session1",
  "ts": "2026-09-22T18:08:51.128Z",
  "from": "claude-session1",
  "to": "human",
  "channel": "hello",
  "subject": "The done/undone curve: the page has one multi-day series, it is day-bucketed, the store is 12.9h old",
  "body": "# Why there is no done/undone curve — the full answer, on the leader's own page\n\n`claude-session1` → `human` (asked the question) and `codex` (lane owner).\n2026-09-23, HEAD `41714bb`. One product file changed this window: **none**.\nThree commits, all `SOP.md`: `46e0102`, `41714bb`, and the earlier `9d64d36`.\n\nYou asked *\"为什么 done、undone 没曲线啊\"*. The short answer is that the page has\n**one** multi-day series, it is bucketed by **day**, and the store is **12.9\nhours long** — so the series is 13 empty buckets and one wall. That is not a\nrendering bug and not a data bug; it is a window wider than the record. Below is\nthe measurement, the reason, and the four copies of the same flat line.\n\n---\n\n## 1. What your page actually draws\n\nPlaywright on `http://127.0.0.1:8777/#/reports`, reading the card text off the\nDOM. The page issues **two** API calls — `/api/state?as=human` and\n`/api/digest?as=human`. It never calls `/api/flow`. Measured:\n`grep -rn \"api/flow\" web/src web/tests` → **0 hits**.\n\n| card | what it prints |\n|---|---|\n| burndown — *remaining work per day, folded from the whole store* | one line: `65 item(s) closed by a recorded move, over 14 day(s).` |\n| flow — *opened vs done, one bucket per minute over the last 60 min* | `4 opened in this window / 48 closed in this window`; `longest quiet run: 18 min`; `nothing recorded for 513 min (last event 05:32 PM)` |\n\nDone and undone appear on this page only as **two horizontal reference lines** on\nthe per-minute chart (`done 139 on the board`, `undone 21`,\n`ReportsPane.vue:571-585`). A horizontal line has no vertical extent by\nconstruction. **The only multi-day series on the page is `reports.series`, and\nthat is the flat one.**\n\n## 2. Why that series is one point — reproduced from the fold, not inferred\n\n    state['tasks']          : 177 rows\n    dated (has `created`)   :  90\n    done  (`moved`→`done`)  :  65\n    created-day histogram   : {'2026-09-22': 90}\n    done-day    histogram   : {'2026-09-22': 65}\n\n`report_data` builds `remaining` as a prefix count over rows carrying a `created`\n(`aimboard/fold.py:326-331`), so with every dated row on one calendar day the\nseries is `0,0,…,0,25` — a single step. The last bucket is `25 = 90 − 65`, which\nis why the burndown header reads 25 while the attention pane reads 21 open: one\ncounts *dated rows with no close*, the other counts *open statuses*, and 4 cards\nwere born terminal and so are open in neither.\n\n**The window is wider than the record, and nothing says so.** The store begins\n`2026-09-22T02:16:37.036Z` and ends `2026-09-22T15:12:08.088Z` — **12.9253\nhours, all on one day.** `report_data`'s window is `days=14` and is never\noverridden; the one production call site passes only `as_of`.\n\n**And even the widest window the page offers proves the point.** Replaying\n`ReportsPane`'s own `flow` computed over `/api/state?as=human` (keeping only\n`created` and `moved`→`done`, 1-minute buckets, anchored on the newest such\nevent `09:32:15.228Z`):\n\n| window | opened | done |\n|---|---|---|\n| 60 min (the default) | 4 | 48 |\n| 360 min | 67 | 63 |\n| 1440 min — the whole day | **90** | **65** |\n\nAt the maximum the page allows, opened reaches 90 and done 65, because 25 of the\n90 dated rows have no close. **A curve with vertical extent cannot exist before\nthere is more than one day of history, and there is not.**\n\n## 3. Four copies of one flat line, three of them dead\n\n| # | where | window | live? |\n|---|---|---|---|\n| 1 | `fold.report_data` → `reports.series` (**what the burndown card draws**) | `days=14` (`fold.py:298`) | **yes** |\n| 2 | `GET /api/flow` → `fold.flow_series` | caller's, real bucketing | routed (`cli.py:543`), **read by nothing** |\n| 3 | `views/reports.py:render_reports` → a server-rendered SVG `<polyline>` | `range(13,-1,-1)` (`:18`) | renders, **unreachable** |\n| 4 | `ReportsPane.vue`'s `flow` computed, client-side from `task.events` | `WINDOWS`, max 1440 min | yes, per-minute only |\n\nCopy 3 is the interesting one. It is still registered —\n`aimboard/views/__init__.py:9` keeps `reports` in `BUILTIN`, and\n`aimboard/page.py` loads it — so it is not dead code, it is **dead surface**.\nRendered directly against the live tree just now:\n\n    rendered 8731 chars · contains <polyline> · max remaining drawn: 25\n    \"65/90 recorded item(s) have a done transition; 87 item(s) are plan seed only\n     and are absent from the chart\"\n\nThat is a fifth number again (87 seed-only vs the board's 177 total) and the same\nwall as copy 1, from a second independent fold of the same data. It is\nunreachable only because `cli.py:887` serves the built SPA whenever\n`web/dist/index.html` exists — which it does. So this is a fourth copy living on\nrather than a path you could have looked at.\n\n## 4. Two cards say \"recorded\" about a list that is not the record\n\n`ReportsPane.vue:84-95` keeps **only** `created` and `moved`→`done`; every other\nkind is dropped before any card on the pane sees it. Two places then use the\nword *record* for that filtered list:\n\n- the flow summary prints `nothing recorded for 513 min (last event 05:32 PM)`.\n  The anchor is the newest `created`/`moved` event (`09:32:15.228Z`), and 513 min\n  is exactly `now − 09:33Z` floored to its minute. **The clock is right and the\n  noun is wrong.** Measured on the live payload: the newest record of *any* kind\n  is `2026-09-22T15:12:08.088Z`, so the real gap is **176 min, not 516** — and\n  **five records were written inside the gap the card calls quiet**:\n\n        14:52:39.225Z  commented  T-0216\n        15:11:23.093Z  commented  T-0219\n        15:11:37.454Z  dropped    T-0178\n        15:11:58.575Z  commented  T-0178\n        15:12:08.088Z  commented  T-0178\n\n- the arrivals card header (`:844`) reads *\"arrivals — newest 14 recorded\n  event(s)\"*, and its rows come from the same filter. `commented`, `assigned`,\n  `published`, `dropped` and `linked` **cannot appear** in a card whose word is\n  \"recorded event(s)\". Live, all 14 listed rows are created/moved, and the five\n  records above appear on no card on the page.\n\nThe pane's own comment thirty lines later says exactly this — *\"`events` counts\nmoves\"* — so the code knows; the two strings above it do not say so.\n\n## 5. A correction to my own earlier report, and to a subagent's\n\n- **Mine.** I reported `/api/flow` totals as `97/65` vs `70/59`. Those are two\n  *seats*, not two surfaces: `/api/flow` is gated per viewer (`cli.py:604-612`),\n  so `as=human` → `opened 97, done 65`; the default seat (mine) → `70/59`.\n  Measured at a third seat (`codex-orangement`) → `39/31`. The client-side\n  derivation from that same seat's `/api/state` gives `97→90` at the leader's\n  seat and `70→63` at mine. **`done` agrees exactly at every seat (65/65,\n  59/59, 31/31); the divergence is confined to `opened`, and it is the same 7\n  everywhere** — six dual-channel ids plus the retracted `T-0001`, which is the\n  table now written down in `SOP.md` §2.1.\n- **A subagent's.** The same agent compared `/api/flow` at one seat against the\n  payload at another and reported the gap as a surface difference. Re-measured at\n  both seats myself; the two-viewer table in §2.1 is the corrected version.\n- **`reports.board_scope` is published under `reports.`, not at top level**, and\n  the store reads it there. At the leader's seat `visible` and `fabric` are\n  numerically identical (177/139/17/21) because `gate.walled_off` returns False\n  for `kind == \"human\"` (`gate.py:24-25`) — the same self-declared master key as\n  `SOP.md` row 11, showing up as a *number* rather than as a permission.\n  `reports.series` and `reports.with_history` do **not** move with the seat, so\n  the burndown card prints the same 65 for every reader while the flow card\n  prints that reader's own 48.\n\n## 6. The minimum fix, and why I did not make it\n\nNot a new feature — a decision about which of two series is *the* series:\n\n1. the burndown card reads `/api/flow` (which already exists, is honest about\n   windows, and has never been read by anything), **or**\n2. `report_data`'s `days=14` becomes a caller-supplied window and the page states\n   the record's actual span, **or**\n3. the per-day fold is left alone and the card says *\"14 day(s) requested; the\n   store is 12.9 h old\"* — the cheapest honest option.\n\n`fold.py`, `views/reports.py` and `web/src/panes/ReportsPane.vue` are codex's\nlane, and which of these is right turns on which series the project means. I have\nchanged none of them. What I did change is `SOP.md`, which now carries the\n`T-0004` seed copy of the leader's blocker, the corrected eight-vs-nine count, and\n`§1.3.2`, the `request-advance` non-validation that produces the dead Approve\nbutton.\n",
  "delivered_via": "claude code",
  "bytes": 8743,
  "body_sha256": "3567c9d98ab9a968eac9cc7f74e67ca913d0592d8fe2c38453a9456ba4b13686",
  "ack_required": false,
  "from_session": "host:notty:pid1062147@2026-09-22T18:08:51.117Z"
}
