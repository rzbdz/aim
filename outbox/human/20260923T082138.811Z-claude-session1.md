# message from claude-session1 -> human
msg_id: 20260923T082138.811Z-claude-session1
channel: hello
sent: 2026-09-23T08:21:38.811Z
subject: Addendum to the 08:13Z report: two numbers in it are wrong (22 -> 26; three -> five)

# Addendum to the 08:13Z report: two numbers in it are wrong

The report `20260923T081342.299Z` is sent and `aim push` hashed its body, so it
cannot be edited — editing it would make it the damaged record the receipt
mechanism exists to detect. Both corrections go here instead. It is the second
time today I have had to do this, which is itself the finding: the two numbers I
got wrong were both counts I had measured at an *earlier* state and written into
a later one, which is the defect this whole pass is about.

## 1. "Twenty-two drafts refuse a move" — it is twenty-six

That paragraph states: *"Twenty-two drafts refuse a move while the channel sits
in `COMMIT`."* No quantity in the store is 22. Measured now, from the product's
own gate:

    aim task list --as claude-session1 --channel hello --count-hidden
    {"visible": 72, "withheld": 26, "phase": "COMMIT", "channel": "hello"}

and measured at **each of the 25 most recent revisions** by running that same
verb against a `git archive` of each: **26 at every one**. There is no revision
at which 22 was the number, so this was not a stale reading — it was a number I
wrote without measuring, in a sentence whose subject I had measured. The related
counts, for the record: 29 open cards, 27 of them drafts, 63 drafts in total.

## 2. "filed three times (`m0002`, `m0003`, and in three outbox reports)" — it is five

Measured, not estimated:

| where | what | when |
|---|---|---|
| `channels/hello/log.jsonl` | `m0002` — the first request | 2026-09-22T09:33:43Z |
| `channels/hello/log.jsonl` | `m0003` — "second request, same edge" | 2026-09-22T20:15:56Z |
| `outbox/human/` | `20260923T070833.262Z` | 07:08Z |
| `outbox/human/` | `20260923T074758.536Z` | 07:47Z |
| `outbox/human/` | `20260923T075322.815Z` | 07:53Z |
| `outbox/human/` | `20260923T081342.299Z` | 08:13Z |

Four reports carry the command verbatim; `grep -c 'aim advance --as human
--channel hello --to CROSS_EXAMINE' outbox/human/*.md` returns 1 for exactly
those four files. `m0002` and `m0003` are **not** ledger rows — the ledger's
`push` events carry `msg_id`, and neither id appears in `ledger.jsonl` or
`tasks.jsonl` at all. They are channel-log ids (`channels/hello/log.jsonl`, the
seed log), so a reader checking that claim against the ledger alone finds nothing
and would be right to call it unverifiable. Say "filed once in the channel log
and four times in outbox reports" if you want it checkable.

The command has not changed:

    aim advance --as human --channel hello --to CROSS_EXAMINE

— `claude-session1`


---
Received it? Confirm with:
  aim confirm --as human --msg-id 20260923T082138.811Z-claude-session1
Reply through the channel, not through this file:
  aim inbox --as human --channel hello
  aim say   --as human --channel hello ...
